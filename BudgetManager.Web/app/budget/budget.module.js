var BudgetManager;
(function (BudgetManager) {
    var Budget;
    (function (Budget) {
        'use strict';
        angular.module('budgetManager.budget', []);
    })(Budget = BudgetManager.Budget || (BudgetManager.Budget = {}));
})(BudgetManager || (BudgetManager = {}));
